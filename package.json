{
  "name": "tripvibz",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TripVibz — static site backed by Supabase",
  "scripts": {
    "predev": "npm install --no-audit --no-fund",
    "dev": "node build.mjs && npx --yes serve dist -l 8080",
    "prebuild": "npm install --no-audit --no-fund",
    "build": "node build.mjs",
    "serve": "npx --yes serve dist -l 8080"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0"
  }
}
